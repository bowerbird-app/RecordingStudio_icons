===============================================================================

RecordingStudioIcons has been installed successfully!

The engine has been mounted at /recording_studio_icons in your application.

The icon registry was designed for RecordingStudio, but it can register defaults for any class or type name in your app.

If you use Tailwind CSS:
1. Run 'bin/rails tailwindcss:build' to rebuild your CSS with RecordingStudioIcons styles

To use the engine:
1. Start your Rails server
2. Visit http://localhost:3000/recording_studio_icons

===============================================================================
